## Built-in widgets.

import nimterm/widgets/[card, containers, diff, input, markdown, menu, panel, question, scroll,
  text, transcript]

export card, containers, diff, input, markdown, menu, panel, question, scroll, text, transcript
